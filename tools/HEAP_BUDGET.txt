==============================================================================
 MAIN RAM BUDGET   generated by tools/heap_budget.py
==============================================================================

---------------
tools/vram_map.py is the single source of truth for VRAM. Until August 2026 there
was no equivalent for main RAM, and there needed to be: adding the Greenhouse
pushed the permanent heap total past the end of the heap, and the console stopped
booting. The failure was not a tidy "malloc returned NULL" - see below - and it
surfaced in a function with nothing to do with the room that caused it, so there
was nothing to read off the crash that pointed anywhere useful.

>>> THE HEAP TOP IS THE STACK. THERE IS NO GUARD. <<<
PSn00bSDK's _start_inner calls InitHeap(_end, 0x801FFFF8 - _end): the heap is
every byte from the end of BSS to the top of RAM. The BIOS stack pointer starts
at 0x801FFF00 and grows DOWN into that same region. Nothing separates them. So
the failure mode when the heap fills is NOT an allocation failure the callers
handle - malloc SUCCEEDS, hands back memory the stack is already using, and the
next CdRead DMAs straight over the return address. The console then jumps into
nothing, which PCSX-Redux reports as "Unrecoverable error while running
recompiler" at whatever PC it last had.

WHAT COUNTS AS PERMANENT
------------------------
  texmgr_register()  keeps the WHOLE TIM in RAM for the life of the run so a room
                     entry can be a pure LoadImage. Never freed. This is by far
                     the biggest consumer and the one that grows per room.
  kept buffers       a model or clip read once at startup whose pointer is stashed
                     in a file-scope variable (the prop SMDs). Never freed.
  NOT counted        loads that are freed again - listed separately as ROOM-SCOPED
                     so nothing vanishes from the report. That covers the scratch
                     buffers (kitchen_stream_textures, fatdoor's two TIMs, the
                     Greenhouse's and the Chain Room's texture streams) AND the
                     boss models, which are read on entry to the room the boss
                     fights in and freed on the way out. Also not counted:
                     room_arena_load, which reads into a BSS array rather than
                     the heap.

>>> A BOSS MODEL IS ROOM-SCOPED, NOT RESIDENT, AND THAT IS LOAD-BEARING. <<<
The Rabisu's RABISU.SMD + RBSIDLE.PVA are 104,448 bytes and WERE read at startup
and never freed, for a boss that exists in exactly one room. That was the single
largest avoidable item in this budget and it was blocking the second boss from
having a model at all. They are now loaded from main.c's STATE_LOADING when the
destination is the Garden Courtyard and freed on every other transition - the
same move room_arena.c made for room meshes, on the heap rather than in BSS
because unlike a room, NO boss is loaded almost all of the time.
Do not give a new boss a startup load. See src/rabisu.c and
tools/ADDING_THE_ASAG_FIGHT.txt PART 6.

The transient allocations still have to FIT in whatever is left, so treat the
"free at rest" figure as the budget, not as slack.

TEXMGR REGISTRATIONS  (resident for the whole run)
  module                   regs      bytes
  --------------------------------------------
  kitchen_dining.c         6         69632
  garden_stairs.c          4         57344
  delivery_area.c          5         51200
  stables.c                4         51200
  attic_exit.c             2         36864
  fountain_square.c        2         36864
  garden_courtyard.c       2         36864
  rafflesia.c              2         36864
  spider.c                 2         36864
  trick_drawers.c          2         36864
  conservatory.c           3         30720
  piano_props.c            3         30720
  maze_one.c               2         28672
  rear_gate.c              2         28672
  outside_catacombs.c      2         24576
  hall_2f.c                2         20480
  attic_stairwell.c        1         18432
  dresser.c                1         18432
  keystone_maze.c          1         18432
  master_bedroom.c         1         18432
  maze_two.c               1         18432
  rabisu.c                 1         18432
  concrete_props.c         1         10240
  east_stairwell.c         1         10240
  piano_room.c             1         10240
  reception.c              1         10240
  TOTAL                    55       765952

DUPLICATE REGISTRATIONS  (the same TIM held in RAM more than once)
  none

KEPT BUFFERS  (read once, pointer stashed, never freed)
  concrete_props.c         CONBLOCK.SMD       4096  -> block_buf
  concrete_props.c         CONCHAIR.SMD       4096  -> chair_buf
  dining_table.c           DINTABLE.SMD       4096  -> table_buffer
  piano_props.c            BOOKCSE.SMD        4096  -> bookcase_buf
  trick_drawers.c          TRKDRWR.SMD        4096  -> drawer_buf
  valve_handle.c           VALVEH.SMD         4096  -> valve_buffer
  vines.c                  VINES.SMD          4096  -> vine_buffer
  chainlink_door.c         CHNLNKDR.SMD       2048  -> cldoor_buf
  dresser.c                DRESSER.SMD        2048  -> dresser_buffer
  fatdoor.c                FATDOOR.SMD        2048  -> fatdoor_buffer
  grinder.c                GRINDER.SMD        2048  -> grinder_buffer
  lever.c                  LEVER.SMD          2048  -> lever_buf
  piano_props.c            PIANO.SMD          2048  -> piano_buf
  piano_props.c            TABLETS.SMD        2048  -> tablets_buf
  save_point.c             SAVEPT.SMD         2048  -> sp_buffer
  TOTAL                                      45056

ROOM-SCOPED LOADS  (read on entry, freed again - NOT permanent)
  rabisu.c                 RBSIDLE.PVA       75776  -> p
  fatdoor.c                WDDR.TIM          34816  -> tim_buf
  rabisu.c                 RABISU.SMD        28672  -> rabisu_buff
  asag.c                   ASGHSLAM.PVA      24576  -> asags_free_model()
  asag.c                   ASGHLAS.PVA       22528  -> asags_free_model()
  asag.c                   ASGHVOM.PVA       20480  -> asags_free_model()
  asag.c                   ASGHFNT.PVA       20480  -> asags_free_model()
  chainlink_door.c         CHNLNK.TIM        10240  -> tim
  fatdoor.c                WDDRCRK.TIM       10240  -> crk_buf
  asag.c                   ASGBODY.SMD        6144  -> asags_free_model()
  asag.c                   ASGHIDLE.PVA       6144  -> asags_free_model()
  grinder.c                GRINDER.TIM        6144  -> tbuf
  TOTAL                                     266240   <- PEAK, not a resting cost

  These cost nothing at rest and everything at the moment they are in.
  The transition that loads them is the peak this heap has to survive,
  so FREE AT REST must stay comfortably above the largest of them.

THE HEAP
  _end (heap start)   0x800E6DF4
  heap top            0x801FFFF8   <- InitHeap; the STACK starts at 0x801FFF00
  heap size            1003340 bytes (980 KB)
  stack floor         0x801DBD40   <- MEASURED $sp; main()'s RenderContext
  stack reserve         148152 bytes (145 KB)  <- NOT usable heap

  texmgr resident       765952 bytes (748 KB)  76.3%
  kept buffers           45056 bytes (44 KB)   4.5%
  PERMANENT TOTAL       811008 bytes (792 KB)  80.8%
  FREE AT REST          192332 bytes (188 KB)  19.2%   <- STACK ALREADY SUBTRACTED
                                  (was 340484 before the stack was counted;
                                   THAT figure is what the old cliff was measured in)

  >>> THIS IS A RESTING TOTAL AND IT DOES NOT MODEL A TRANSITION'S PEAK.
      THAT IS WHAT KILLS YOU, AND IT IS PER-ROOM. <<<
      The GARDEN COURTYARD is the tightest door in the game: it is the
      only transition that loads SND_BANK_BOSS, whose EMERGE clip is a
      71,680-byte malloc — the largest single transient anywhere here —
      and the Rabisu's 104,448-byte model is loaded at the same door.
      Held together that is 176,128 bytes at one instant; main.c now
      SEQUENCES them (free before the bank swap, load after) so the peak
      is 104,448. Work the peak out by hand for any room you touch.
      See tools/DIAGNOSING_A_BOOT_CRASH.txt section 8.

  >>> AND THE ~234 KB CLIFF IN THAT DOCUMENT WAS A UNITS BUG. <<<
      It was measured against a FREE AT REST that did not subtract
      main()'s 145 KB RenderContext stack frame, and was ~16 KB out on
      top of that for the TIM-scratch miscount. In the units printed
      above, that cliff is about 74 KB - i.e. it was simply the heap
      being full, not a threshold. Asag's arena proved it in September
      2026: 229 KB of boss loads against a reported 361 KB free crashed
      inside CdReadSync with $sp INSIDE the buffer being read. See
      tools/ADDING_THE_ASAG_FIGHT.txt PART 6A.

  The largest single transient allocation still has to fit in what is
  free, and startup makes several: the kitchen's 32768-byte scratch, and
  every read_file above (largest 75776 bytes). Keep FREE AT REST well clear
  of six figures. >>> AND IT IS STILL A RESTING TOTAL. <<< A transition
  can hold several of the loads above at once, and the room whose door
  is tightest is not the room you are editing. Work the peak out by
  hand: permanent, plus everything live at that one instant.
  MEASURED at the tightest door in the game (Asag's arena, where the
  boss model is read): 121-147 KB actually free, against the 187 KB
  printed above. The difference is other transients still held at that
  moment. Treat anything under 128 KB free as already in trouble.

  OK: comfortable margin.

Regenerate with:  python tools/heap_budget.py > tools/HEAP_BUDGET.txt
