PS1 HORROR GAME — GIT REPOSITORY DETAILS
==========================================
For use by Claude and the developer when pushing, pulling, or committing.


REPOSITORY
----------
Remote URL:   https://github.com/MattyMcHatty/ps1game.git
Branch:       master
Git username: virtu
Git email:    virtuousvolgin@gmail.com


BASIC COMMANDS (run from project root: C:\Users\virtu\Documents\ps1game)
-------------------------------------------------------------------------
Pull latest changes from GitHub:
    git pull

Push committed changes to GitHub:
    git push

Check what has changed (unstaged, staged, untracked):
    git status

See a summary of recent commits:
    git log --oneline -10


COMMITTING CHANGES
------------------
Always stage and commit files from these folders when they have changed
or when new files have been added to them:

    assets/          — level meshes (.smd), prop meshes, other game assets
    blender models/  — source .blend files for any 3D models
    misc/            — editor project files for other source media, e.g. the
                       Audacity .aup3 a voice clip was cut in. Same reasoning
                       as "blender models/": it is the editable original that
                       sounds/*.wav was rendered from, and it is not
                       reproducible from anything else in the repo.
    music/           — background music tracks (.wav, .mp3, etc.)
    sounds/          — sound effects (.vag, .wav, etc.) and the conversion
                       scripts beside them (mp3_to_wav, wav_to_vag, gain_wav,
                       crush_wav)
    src/             — all C source files (.c) and headers (.h)
    textures/        — texture files (.tim, .png, source images)
    tools/           — the runbooks (ADDING_A_*.txt, *_NOTES.txt), the VRAM
                       map and the Blender exporters/generator scripts

Also always commit these specific files if they have changed:
    CMakeLists.txt          — build configuration
    disc.xml                — disc layout (controls what goes on the disc)
    system.cnf              — PS1 boot config
    rebuild commands.txt    — build instructions
    git_details.txt         — this file
    .gitignore              — controls what git ignores

When committing, stage everything relevant at once rather than file by file.
Example full commit workflow:

    git add src/ textures/ assets/ sounds/ music/ tools/ CMakeLists.txt disc.xml
    git commit -m "Your description of what changed and why"
    git push

To also pick up NEW files in those folders (git add stages new files too):
    git add src/ textures/ assets/ sounds/ music/ tools/ misc/ "blender models/"
    git add CMakeLists.txt disc.xml system.cnf "rebuild commands.txt" git_details.txt .gitignore
    git commit -m "Description"
    git push

A multi-line commit message will not survive `-m` on this machine's shell —
write it to a file and use `git commit -F <file>` instead.


WHAT IS INTENTIONALLY IGNORED (.gitignore)
-------------------------------------------
These are excluded from git and should NOT be committed:

    build/       — all compiled output (HORROR.exe, HORROR2.BIN, object files)
    *.exe        — compiled ELF binaries
    *.bin/*.BIN  — raw PS-EXE and disc images (large, always re-generated)
    *.cue        — disc cue sheets (re-generated by mkpsxiso)

The disc image (build\HORROR.bin + build\HORROR.cue) is always rebuilt
locally using mkpsxiso — it does not need to be in version control.


NOTES FOR CLAUDE
----------------
- IMPORTANT: Never stage, commit, or push anything unless the user explicitly
  says "commit" or "commit and push". Do not commit as part of completing a
  task unless directly instructed.
- The repo is on GitHub at https://github.com/MattyMcHatty/ps1game.git
- Branch is always "master" — do not create new branches unless asked.
- When asked to "commit and push", stage all changed and new files from
  the folders listed above, write a clear commit message summarising what
  changed and why, then push to origin/master.
- Co-author line for commit messages:
      Co-Authored-By: Claude <noreply@anthropic.com>
- Do not commit build/ output. Do not commit *.bin, *.BIN, *.exe, *.cue.
- If in doubt about whether a file should be committed, ask. A good rule
  of thumb: if a human created or edited it, commit it. If a tool
  generated it automatically from other committed files, don't commit it.
